import styled from '@emotion/styled';

const DropdownMenuSeparator = styled.hr`
  margin: 0.25rem 0;
  border: none;
  border-top: 1px solid var(--color-gray-200);
`;

export default DropdownMenuSeparator;
